package com.talestra.talesof.complib

import org.junit.Test

class complibtest {
	@Test
	fun name() {
		//val BIn = byteArrayOf(1, 1, 1, 1, 1, 1, 1, 1).toByteArraySlice().getPointer()
		//val BOut = ByteArray(32).toByteArraySlice().getPointer()
		//val slice = Encode(1, BIn, 8, BOut, 32)
		//println(slice)
	}
}